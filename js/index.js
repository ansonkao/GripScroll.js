var GripScroll = (function($){

  // ==========================================================================
  // Config
  // ==========================================================================
  $.prototype.config = $.prototype.config || {};

  $.prototype.constructor = function( el ){
  };


  return $;

})(GripScroll || {});